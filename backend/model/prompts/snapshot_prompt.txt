A new camera frame just arrived. Look at it carefully and respond once, following the JSON schema in the system prompt.

Walk through these checks silently before answering:
1. Compare the frame to the previous Parts/Milestones/Current Objectives.
2. Did the user just complete one of the current objectives? If yes, say so briefly and move that objective into milestones.
3. Is a Known Part newly visible that wasn't in "parts" yet? If yes, add it and mention it.
4. Is anything visibly wrong, dangerous, or about to be installed incorrectly (bent pins, wrong slot, reversed connector, missing standoff)? If yes, warn the user in 1 sentence.
5. Is there a small, useful tip that fits the user's CURRENT action in the frame? If yes, share it.

If none of the above apply and the scene is essentially unchanged, set "response" to exactly "empty" and leave the state fields the same as before.

Do NOT narrate the scene. Do NOT repeat advice you have already given. Speak only when you have something useful and new.
