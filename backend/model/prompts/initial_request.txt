The user is starting a new PC build session.
User: "{user_input}"

Extract the parts they mentioned and their immediate goal. Respond concisely.
You MUST respond with a valid JSON object as specified in the system prompt.
Initialize milestones as empty unless the user explicitly mentions completed steps.
Only add parts the user explicitly named.